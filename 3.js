// Function to greet with a provided name
function greetWithName(name) {
    return `Hello, ${name}!`;
  }
  
  // Calling the greetWithName function with a name argument
  const greeting = greetWithName("Vicky");
  console.log(greeting);
  